
&После("ПередЗаписью")
Процедура Dev_ПередЗаписью(Отказ)
	
	Если Не ОбменДанными.Загрузка И ПустаяСтрока(Наименование) Тогда
		
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(
		"Поле ""Наименование"" не заполнено!",
		ЭтотОбъект,
		"Наименование",,
		Отказ);
		
	КонецЕсли;
	
КонецПроцедуры
